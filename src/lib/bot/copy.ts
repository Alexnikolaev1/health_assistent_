/**
 * Единое место для юридически значимых формулировок и повторяющихся текстов бота.
 */

/** Короткий дисклеймер — в /start и справке */
export const MEDICAL_DISCLAIMER_SHORT =
  '⚠️ _Сервис носит информационный характер и не заменяет очный приём врача и диагноз._';

/** Полный блок для ответов AI по симптомам */
export const MEDICAL_DISCLAIMER_FULL =
  '\n\n⚠️ *Важно:* ответ носит справочный характер, не является медицинским диагнозом и не заменяет консультацию специалиста. При ухудшении состояния, сильной боли, высокой температуре, одышке, неврологических симптомах — обратитесь к врачу или вызовите скорую (112).';

/** Что сделать, чтобы уведомления совпадали с часами на телефоне + предупреждение про МСК по умолчанию */
export const TIMEZONE_HELP_BLOCK =
  `⏰ *Время уведомлений*\n` +
  `Бот *не получает* часовой пояс из мессенджера — его нужно указать один раз: команда /timezone ` +
  `(город из списка IANA или смещение UTC±, как в настройках «Дата и время» на телефоне).\n\n` +
  `⚠️ *Если пояс не задать*, напоминания и ежедневные привычки по расписанию считаются по *московскому времени* (МСК). ` +
  `Если вы живёте не в часовом поясе Москвы — выполните /timezone, иначе уведомления придут «не в тот час».`;

/** Короткая сноска после создания напоминания */
export const REMINDER_TIMEZONE_FOOTNOTE =
  `\n\n_Время — в часовом поясе из /timezone. Если /timezone не настраивали — используется московское время (МСК)._`;

/** Верх экрана /timezone без аргументов — предупреждение про МСК */
export const TIMEZONE_COMMAND_HEADER =
  `Если ваше местное время *не московское*, задайте пояс — иначе напоминания и ежедневные привычки по расписанию считаются по *Москве* (МСК).\n\n`;

export function buildWelcomeBody(firstNameSuffix: string): string {
  return (
    `👋 Привет${firstNameSuffix}! Я — *Твой здоровый MAX*.\n\n` +
    `${MEDICAL_DISCLAIMER_SHORT}\n\n` +
    `Я помогу тебе:\n` +
    `🤒 Сориентироваться по симптомам (AI, без диагноза)\n` +
    `📊 Вести дневник здоровья\n` +
    `⏰ Не забывать о лекарствах\n` +
    `💪 Формировать полезные привычки\n` +
    `👨‍⚕️ Подсказка для визита к врачу\n\n` +
    `🕐 *Уведомления по времени:* чтобы они совпадали с часами на телефоне, один раз укажи часовой пояс — /timezone. ` +
    `*Если этого не сделать*, время напоминаний считается по *Москве* (МСК).\n\n` +
    `Просто опиши симптомы или выбери действие:`
  );
}

export const HELP_TEXT =
  `❓ *Как пользоваться:*\n\n` +
  `🤒 *Симптомы* — опишите что беспокоит; AI даст ориентир, не диагноз\n` +
  `📊 *Метрики* — давление, пульс, сахар и т.д.\n` +
  `⏰ *Напоминания* — лекарства и процедуры\n` +
  `💪 *Привычки* — трекинг и челленджи\n` +
  `👨‍⚕️ *Визит к врачу* — что сказать специалисту\n\n` +
  `${MEDICAL_DISCLAIMER_SHORT}\n\n` +
  `${TIMEZONE_HELP_BLOCK}\n\n` +
  `*Быстрый ввод метрик:* _давление 120/80_, _пульс 75_\n\n` +
  `*Команды:*\n` +
  `/symptom — анализ симптомов\n` +
  `/metrics — дневник\n` +
  `/reminder — напоминания\n` +
  `/habits — привычки\n` +
  `/timezone — задать часовой пояс (нужно для правильного времени уведомлений)\n` +
  `/appointment — подсказка перед визитом\n` +
  `/sickleave — справка про больничный\n` +
  `/cancel — отменить текущий шаг диалога`;

export const CANCEL_OK =
  '❌ Сценарий отменён. Можете снова выбрать действие в меню или написать /help.';
